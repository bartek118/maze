Just an easy maze generator based on the DFS algorithm. 

To compile just use 
make all

The usage:
./maze to generate a maze of size 20
./maze n to generate a maze of size n, where n is a positive integer
